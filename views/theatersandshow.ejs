<!DOCTYPE html>
<html>
<head>
  <title>theater and shows</title>
  <style>
    /* Add your CSS styles here */
    .card {
      border: 1px solid #ccc;
      border-radius: 5px;
      padding: 10px;
      margin: 10px;
      display: inline-block;
      width: 300px;
    }

    .card img {
      width: 100%;
      height: 250px;
      object-fit: cover;
      border-radius: 5px;
    }

    .card h2 {
      margin-top: 10px;
      font-size: 24px;
      font-weight: bold;
    }

    .card p {
      margin-top: 5px;
      font-size: 16px;
      color: #666;
    }
  </style>
</head>
<body>
    <h1>Movies</h1>
    <% info.result.forEach(theater => { %>
      <div class="card">
        <form method="post" action="/theater/<%= theater.theater_id %>" >
          <h2><%= theater.theater_name %></h2>
          <p><strong>Language:</strong> <%= theater.city %></p>
        </form>
        <% info.shows.forEach(show => { %>
            <% if(theater.theater_id == show.theater_id){ %>
                <form method="post" action="/shows/<%= show.screen_id %>" >
           
                    <p><strong>Timings:</strong> <%= show.Timings %></p>
                    <p><strong>Date:</strong> <%= show.show_date %></p>
                    <p><strong>Screen No:</strong> <%= show.screen_no %></p>
                    <p><strong>Screen Dimensions:</strong> <%= show.screen_dimensions %></p>
                    <button type="submit">Booknow</button>
                  </form>
           <%   } %>
         
        <% }) %>
      </div>
    <% }) %>
    


  </body>
  
</html>
